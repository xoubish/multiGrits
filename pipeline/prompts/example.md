Read `talk-context.md` and the simple-example entry in `slides/outline.md`. Build the example under `examples/`
and run it, as described in your agent instructions. Budget for the example's own run: $1.

Write `examples/<name>/RESULT.md` and `{{RUN_DIR}}/notes.md`. Return at most 120 words.

Read `talk-context.md`, `slides/outline.md`, `slides/deck.md`, `research/build-log.md`, and `{{CRITIQUE}}/review.md`.
If `research/build-log.md` is newer than the deck's account of the build (section 3 lists runs the deck does not
mention), update the build-story slides from it first, copying numbers and file text exactly.

Apply every must-fix finding; use judgment on nice-to-have. Never change the outline's slide order or count; the
`## For the speaker` section is not yours to act on. Keep every rule in your agent instructions (full sentences,
complete citations, about 60 words per slide, split rather than shrink). Edit `slides/deck.md` in place.

Write `{{RUN_DIR}}/changes.md`: one line per finding, changed or declined and why. Return at most 100 words.

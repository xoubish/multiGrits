# Changes — run 024 (revise, placing the second chronicle pass) against runs/021-critique/review.md

Build log first: `research/build-log.md` section 3 now covers runs 017–023; the deck stopped at run 016 with a placeholder. Entry 19 went from 2 slides to 5 (runs 017/018, 019/020, 021/022, 023 and the unrun stages), one line per run with agent, model, what it read or wrote, turns, dollars, seconds, copied from the log. Entry 20 slide 1 now reads $14.7838 / 328 turns / 1,593 s for runs 015–022 and about $44.00 running total (log section 5); the $1.0495 / $30.27 figures through run 016 are gone. Notes on those slides carry the per-worktree numbers for run 018 and the "loop verdict not recorded" fact.

1. Slide 52 / slide 1 bg crop (must-fix) — already changed in run 022 (`![bg right:45% fit]` on both); verified present, no further change.
2. Slide 41 Tran & Kiela title (must-fix) — already changed: the title is on the slide and the Sources slide, with a note that the fact-checker verifies it; no further change.
3. Slide 39 diminishing returns (must-fix) — already changed in run 022 to the speaker's inference ("my reading of their results, not a number from the paper"); `TODO evidence:` stays in the notes.
4. Slide 46 fourteen-line code block (must-fix) — already changed in run 022: entry 24 is five slides, rules and return format on separate slides with a sentence each.
5. Slides 13 and 20 diagram labels (must-fix) — writer-critic diagram is already full width (run 022); meta-pipeline is full width and its label size is a diagrammer fix (Mermaid font or shorter edge labels), which I cannot make without writing to `diagrams/`. Declined for that half; noted on the slide.
6. `.cite` 24px (nice-to-have) — already changed in run 022; verified in the style block.
7. Slide 35 placeholder (nice-to-have) — changed: replaced with the recorded runs, see above.
8. Slides 26–27 screenshots too small (nice-to-have) — declined: cropping the PNGs means editing `slides/shots/`, which is not my output path and needs an image tool; they stay at w:560.
9. Slide 33 `revise` stage unexplained (nice-to-have) — already changed in run 022 ("then one revise pass places the new build-log entries"); no further change.

For the speaker items (eleven vs fourteen roles, cuts 1/3/4, entry 25's job) — not acted on, per instructions.

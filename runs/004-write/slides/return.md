Both files are written; nothing else is pending, so no further tool calls.

**Summary**

`slides/deck.md` replaces the skeleton with a 24-slide Marp deck (limit 25): the outline's 21 slides in order, plus three Sources slides carrying full URLs, split so each stays under the 40-word body cap. Every slide has speaker notes in an HTML comment with segment timing; demo slides 11–15 include the fallbacks. Five diagram tokens are used, one per line; nothing drawn, nothing written to `diagrams/`. All 23 bracketed tags resolve on the Sources slides, and nothing from the brief's Unverified list appears.

Two things I verified against the repo rather than the outline: `stage_write` names the worktrees `slides`/`diagrams` (slide 13 now matches the terminal), and the critic's criterion 6 wording quoted on slide 14 is exact.

`runs/004-write/slides/notes.md` logs the count, constraint checks, deliberate deviations, the three least-confident slides (6, 14, 9), and items for the fact-checker — chiefly the disputed Anthropic post URL.

`slides/outline.md` is written: 21 slides across the five fixed segments, content budgets summing to exactly 1500s (Demo held at a literal 12 min for clean minute markers; Why/Patterns/Gotchas scaled proportionally to absorb talk-context's own 27-vs-25-minute table/prose mismatch, logged transparently), plus a 300s Q&A/hand-off. Slide 2 is the required meta-demo framing slide. The four pattern slides (fan-out, pipeline, writer-critic, parallel workers) are marked **[DIAGRAM]**. The demo segment (slides 11–15) gives real minute-by-minute screen actions grounded in this actual repo (run 001's fan-out logs, `pipeline/run.sh`'s worktree stage, a live `pipeline/run.sh critique` call, then the cost report), with a stated fallback to pre-logged runs if the live call fails or hits budget. Checked every slide pair and the non-goals list for duplication/overlap. Ends with an ordered "Cuts if running long" list.

Logged five items to `runs/002-outline/notes.md`: a talk-context timing inconsistency (not the research brief) I had to resolve, the "script the orchestration" gotcha's thin (non-quantified) evidence, an unverified MAST sub-attribution I deliberately excluded, an unbacked time estimate for the live critic slide, and a deliberate omission of the tools-comparison table to avoid a Day-1 non-goal.

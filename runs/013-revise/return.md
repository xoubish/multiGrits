Revised `slides/deck.md` in place and logged every finding in `runs/013-revise/changes.md`.

**Applied (all must-fixes within my files):** three build failures now on slide 16 body and slide 13 notes with `[run 002]`/`[run 006]` tags; literal `claude -p --agent … --max-budget-usd 5` command on slide 12; objectives line on slide 2; first-step line on slide 26 with a one-line BJ hand-off; Sources 4 rebuilt with repo URL and tags for runs 004/007/008/009, cost report; shot images moved from `h:470` to a CSS bounding box (full width per talk-context), text slides vertically centred; demo-slide clocks; shape tags on pattern titles; Osmani moved to slide 23; MAST defined.

**Declined, flagged:** diagram edits (diagrammer), outline (outliner), handout fixes (notes-writer), publishing the repo (Shooby). Slide count/order unchanged, so the speaker script was not touched.

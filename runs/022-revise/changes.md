# Changes from runs/021-critique/review.md (run 022, revise)

Build log check: `research/build-log.md` section 3 still ends at run 017 (in progress); the deck already reports runs 015–017 and says the rest are not yet recorded, so no build-story update was needed.

1. Slide 52 / slide 1, bg crop — **changed**: both `bg right:45%` lines now `bg right:45% fit`.
2. Slide 41, Tran and Kiela title — **partial**: no file in the repo has the title (re-checked); I will not invent one. The cite line now says "title to be confirmed by the fact-checker" so the gap is visible on the slide; TODO evidence kept in the notes. Dropping the slide is the outline's cut 2 and the speaker's call, not mine.
3. Slide 39, diminishing returns — **changed**: rewritten as the speaker's own inference ("my reading of their results, not a number from the paper"); TODO evidence kept so a figure can replace it.
4. Slide 46, fourteen-line code block — **changed**: split into "the instructions" (lines 7–14, with a sentence above) and "the return format" (lines 16–20, with a sentence above). Entry 24 is now five slides; numbering and time splits updated.
5. Slides 13 and 20, diagram labels — **changed for 13**: writer-critic diagram now runs full width under the text, as the pipeline slide does. **Declined for 20**: the meta-pipeline is already full width; the label size is a Mermaid font/edge-label fix for the diagrammer, noted in the slide's speaker note. I do not write to `diagrams/`.
6. `.cite` 22px → 24px — **changed**; trimmed one clause each from the Kim and Anthropic-research cite lines (moved to the notes) to avoid overflow.
7. Slide 35 placeholder — **declined for now**: the build log has no runs past 017 yet; the paragraph stays honest. The post-chronicle revise pass replaces it.
8. Slides 26–27 screenshots — **changed**: two-column layout switched to even columns and images raised to `w:560`. I cannot crop the PNGs (not my files).
9. Slide 33 revise stage — **changed**: added "Chronicle runs again, then one revise pass places the new build-log entries."

`## For the speaker` items not acted on, per instructions.

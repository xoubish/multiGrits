# Changes — run 029 revise, from runs/028-critique/review.md

Preliminary check: `research/build-log.md` section 3 covers runs 015–022 and names itself as run 023; the deck already tells the build story through run 023, so the log is not newer than the deck's account and no build-story renumbering was made. Runs 025–027 and the $49.19 total named under "For the speaker" are not in the build log, so they were not added (the deck copies history only from the log).

## Must-fix

1. Footer and slide 1 — changed. Footer is now "Multi-agent workflows · GRITS AI workshop, IPAC · September 2026"; title subtitle is "GRITS AI workshop, IPAC, September 2026."; title-slide transition note no longer says "Yesterday".
2. Slide 2 — changed. Retitled "One collaborator"; body now assumes prior use of a coding agent such as Claude Code or Codex on real work and states that the question of the talk is when a second agent is worth having; no session or speaker names, no "yesterday".
3. Slide 3 — changed. Fragments replaced with "A single coding agent already plans, writes, checks, and debugs on request."; "So why not a dozen?" and "I want to pose that question..." replaced with "the obvious next step is a team of them. This talk poses that question before answering it."
4. How many agents? (slide 16) — changed. Opens "One agent is the default in current tools."; note records that the citation covers Claude Code only.
5. Cost (2) — changed. Sentence naming a speaker replaced with "Model selection and pricing are covered elsewhere in the workshop and are not repeated here."; "point to Nick" removed from notes; the `.cite` clause "unless you define a custom Explore" reworded to "unless a custom Explore is defined", numbers and source unchanged.
6. Close — changed. "Your first step on Monday" is now "A first step is to..."; hand-off reads "which is the subject of the following session."; notes say "hand off to the following session" with no name.
7. Entry 24 titles — changed. All five now "A simple example to run first", "(2): the instructions", "(3): the return format", "(4): the command", "(5): the real result"; numbering scheme kept.
8. Register sweep — changed on every slide body, meaning, numbers, citations, code blocks, diagrams and illustrations kept. Examples: "let me fix the words" → "Definitions first"; "So I reset." → "The pipeline was reset."; "You saw my cost log." → "the shared cost log in attempt one is an instance"; "That is how you audit a pipeline" → "That is how a pipeline is audited rather than trusted"; "Agents optimize the rubric you write" → "the rubric they are given"; "which is the one you are reading" removed; "you will see shortly" → "shown in the simple example"; "Everything you are looking at" → "Every part of this deck"; "If you have run the same task..." recast in the third person with "the human as orchestrator"; imperatives on the task-shape and cost slides recast as declaratives; MAST category fragments joined into one sentence; the RULER cite's "I am not merging their numbers" → "their numbers are not merged here". First person kept where the speaker's own actions are narrated (my segment table, I wrote this outline, I own the outline, my own inference, most of my own work) and in the outline's own entry-3 wording ("the best collaborator I have had"). "Context, not intelligence." and "two agents, one file, last write wins" kept as the outline's own phrasing. The compact run-cost tails on the attempt-two slides ("37 turns, $0.7286, 171 seconds.") were kept: the outline asks for one line per run and expanding each into a sentence pushed those slides past 60 words.
9. Speaker notes — changed. No other speaker's name remains anywhere in the deck; transitions refer only to "the following session". Notes on the reset slide add a TODO: the outline's header records that entries 1, 2, 24, 26 and 27 were reworded on 2026-09-18, while the quoted entry-1 code block still shows the wording `research/build-log.md` section 4 records ("GRITS Day 2, advanced track"). The block was kept because the review says code blocks do not change and the build log is the only source for build-story text; the prose above it now says "as the build log records it" rather than "exactly as it sits in slides/outline.md". To refresh when the chronicler next runs.

## Nice-to-have

10. "For." / "Against." / "Pattern one is ..." leads — changed. Evidence slides now open "The case in favor comes from..." / "The case against comes from two papers. In the first, ..." / "In the second, ..."; pattern slides open "Pattern 1, fan-out and merge, ..." through "Pattern 4, parallel isolated workers, ...".

## For the speaker

Not acted on, per instructions: superseded run totals (runs 025–027, $49.19), the entry-19 cut candidate, and the "(2)"/"(3)" title numbering.
